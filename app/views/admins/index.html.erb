<p id="notice"><%= notice %></p>

<h1>Administrador</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Password</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @admins.each do |admin| %>
      <tr>
        <td><%= admin.nombre %></td>
        <td><%= admin.password %></td>
        <td><%= link_to 'Mostrar', admin %></td>
        <td><%= link_to 'Editar', edit_admin_path(admin) %></td>
        <td><%= link_to 'Eliminar', admin, method: :delete, data: { confirm: '¿Estas seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nuevo administrador', new_admin_path %>
